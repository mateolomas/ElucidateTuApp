import { ScrollView, Text } from 'native-base'
import React from 'react'

const Details = () => {
  return (
    <ScrollView>
        <Text>Details</Text>

        </ScrollView>
  )
}

export default Details