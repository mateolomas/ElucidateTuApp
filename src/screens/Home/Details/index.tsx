import React from 'react';
import {ScrollView, Text} from 'react-native';

const Details = () => {
  return (
    <ScrollView>
      <Text>Details</Text>
    </ScrollView>
  );
};

export default Details;
