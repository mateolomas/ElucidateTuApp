import {ScrollView, Text} from 'native-base';
import React from 'react';

const Profile = () => {
  return (
    <ScrollView>
      <Text>Profile</Text>
    </ScrollView>
  );
};

export default Profile;
